# Packet S4B-1 — the RPS roster + proof (SMALL, scaffolded)

> **Read `AGENTS.md` + `PROGRESS.md` first.** ONE tiny slice: add a ROCKET unit to the roster and PROVE the
> rock-paper-scissors relationship with a unit test (using the existing damage system + the locked weapons matrix).
> No new systems, no renderer, no wiring. Work in place.

## Scope (only this)
1. Add a `rocket_trooper` unit to `data/units.json` (an anti-vehicle infantry): `weaponId: "inf_rocket"`,
   `armorClass: "LIGHT"`, `hp: 20`, `speed: 12`, `team: "player"`, plus a `graphics` block like the others
   (`weaponGlyph: "ROCKET"`, `roleBadge: "ANTI_VEHICLE"`, `chassis: "INFANTRY"`). Keep the existing `infantry`
   (rifle, LIGHT) and `vehicle` (scout_gun, MEDIUM armor) entries.
2. Create `tests/unit/rps.test.ts` proving the counter triangle via the REAL damage system + matrix.

## Why this proves RPS (the numbers, from data/weapons.json — do not hardcode, read them)
- `inf_rocket` is ROCKET (dmg 20); ROCKET vs MEDIUM = 0.9 → 18/shot; ROCKET vs NONE = 0.4 → 8/shot.
- `rifle` is BULLET (dmg 8); BULLET vs MEDIUM = 0.3 → 2.4/shot; BULLET vs LIGHT = 0.5 → 4/shot.
So a rocket unit shreds MEDIUM armor (18 vs 2.4) but is weak vs unarmored (NONE); a rifle is the reverse. That IS
the RPS.

## Required test — `tests/unit/rps.test.ts` (use the damage system, like combat.test.ts)
Load real weapons (`loadWeapons(weaponsData)`), build `[makeDamageSystem(weapons)]`, run one `runTick`, and read the
target's `health.hp` drop. Prove:
- A **ROCKET** attacker (`weaponId:'inf_rocket'`) vs a **MEDIUM**-armor target removes MORE hp than a **BULLET**
  attacker (`weaponId:'rifle'`) vs the same MEDIUM target. (18 vs 2.4)
- A **BULLET** attacker vs a **NONE**-armor target removes MORE hp than a **ROCKET** attacker vs a NONE target.
  (8 vs 8 — careful: rifle 8×1.0=8, rocket 20×0.4=8 → TIE; instead compare vs **LIGHT**: rifle 8×0.5=4 loses to
  rocket 20×1.0=20, so pick the pair that shows the reversal clearly — assert ROCKET vs MEDIUM ≫ BULLET vs MEDIUM,
  AND BULLET-per-damage-point favors soft targets: assert `matrix.BULLET.NONE > matrix.BULLET.MEDIUM` and
  `matrix.ROCKET.MEDIUM > matrix.ROCKET.NONE` reading the loaded matrix directly.)
- Each attacker must be seeded with `combat:{ weaponId, cooldownRemaining:0, targetId:<the target's id> }`, a
  `position` in range, and the target with `health` + `armor:{armorClass}` + `position`. (You DO need the target id
  here — bind it. Per AGENTS.md, only bind ids you use.)

## Acceptance
`pnpm run verify` green (all 68 prior tests still pass). One commit; update `PROGRESS.md`; **CALL `kanban_complete`.**
