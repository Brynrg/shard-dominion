# Packet S1 — economy visible: the harvester FSM + credits

> QUEUED behind S0. Do not start until S0 is shipped, reviewed, and you are handed this packet.
> **Read `PROGRESS.md` and `BUILD_CONSTITUTION.md` first.**

## Goal
A player watches credits **rise on deposit** and a shard field **deplete**, driven by a real harvester economy.
The HUD shows credits + the harvester's cargo. Overflow beyond storage is shown as an exact loss.

## You MAY edit only these files
```
src/sim/systems/harvest.ts        (create — the harvester FSM)
src/sim/systems/movement.ts       (extend ONLY if the FSM needs to set movement.target; keep it minimal)
src/view/hud.ts                   (create — credits + cargo + storage readout)
src/view/**                       (only the HUD wiring)
src/main.ts                       (only to seed shard tiles + refinery storage + player credits)
tests/liveness/s1.spec.ts         (create)
tests/unit/economy.test.ts        (create — FSM behaviour)
```
**Do NOT touch** the immutable contract layer. **Do NOT** add base building, combat, selection, or the planet.

## Contract / data to use
- Numbers come from data, never literals: extract 25 density/s, capacity 700, dock 100 cr/s, refinery store 2000
  — put them in `data/economyConstants.json` behind a zod schema in `src/loaders/schemas.ts` *(adding a new data
  file + schema is allowed; editing existing contract files is not — if a schema needs a new entry, add a new
  schema, don't mutate a pinned one)*.
- Harvester FSM: **SEEK** (densest reachable shard, via `state.grid`) → **HARVEST** (drain tile density, fill
  cargo) → **RETURN** (nearest refinery with a free dock) → **DOCK** (move cargo into player credits at 100/s;
  overflow beyond storage is LOST and shown).
- The economy is a `SimSystem` named `'harvest'`; it runs in `SYSTEM_ORDER` after `movement`.

## Acceptance (paste real output)
- `pnpm run verify` green; new `economy.test.ts` proves: cargo only rises while harvesting; credits only rise on
  deposit; tile density falls; overflow is lost; no two harvesters deadlock on one dock.
- S1 liveness gate: rendered credits-HUD pixels **change** as deposits land; a shard tile's density visibly
  drops; screenshot saved. (The S0 motion assertions still pass.)
- One/few conventional commits. Update `PROGRESS.md`. **STOP** for review.

## Out of scope
Base building, power, combat, selection beyond what S0 gave, fog, AI, the planet, art.
